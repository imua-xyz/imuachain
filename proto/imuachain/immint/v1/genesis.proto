syntax = "proto3";
package imuachain.immint.v1;

import "imuachain/immint/v1/params.proto";
import "gogoproto/gogo.proto";

option go_package = "github.com/imua-xyz/imuachain/x/immint/types";

// GenesisState defines the immint module's genesis state.
message GenesisState {
  // params refers to the parameters of the module.
  Params params = 1 [(gogoproto.nullable) = false];
  // there are no other constituents of the state.
}
